<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('partials/fonts') %>
        <title>Reset Password</title>
        <link rel="stylesheet" href="/style.css">
</head>

<body class="font-[Inter] flex justify-center p-6 items-center min-h-screen bg-gray-50 selection:bg-[#6d74fc] selection:text-white">
    <div class="">
        <!-- Alert -->
        <%- include('partials/success') %>
        <h2 class="text-xl font-semibold text-[#6d74fc] ">Create New Password</h2>
        <p class="text-sm text-gray-500 mt-1">
            This password should be different from the previous password.
        </p>

        <form class="mt-4">
            <input type="hidden" id="token" name="token" value="<%= token %>">

            <!-- New Password -->
            <div class="relative">
                <input type="password" id="password" name="password" placeholder="New password" minlength="8" required
                    class="w-full p-3 border rounded-lg focus:outline-none focus:ring-2 focus:ring-[#6d74fc]">
                <span class="absolute right-3 top-3 cursor-pointer text-gray-400" onclick="togglePassword('password','eyeImage', 'CloseEyeImage' )">
                    <img src="/eye-svgrepo-com.svg" alt="eye" id="eyeImage" class="h-5 w-5">
                    <img src="/eye-password-hide-svgrepo-com.svg" alt="closeEye" id="CloseEyeImage" class="h-5 w-5 hidden">
                </span>
            </div>

            <!-- Confirm Password -->
            <div class="relative mt-3">
                <input type="password" id="confirmPassword" name="confirmPassword" placeholder="Confirm password" minlength="8" required
                    class="w-full p-3 border rounded-lg focus:outline-none focus:ring-2 focus:ring-[#6d74fc]">
                <span class="absolute right-3 top-3 cursor-pointer text-gray-400"
                    onclick="togglePassword('confirmPassword','ConfirmPasswordEyeImage', 'ConfirmPasswordCloseEyeImage')">
                    <img src="/eye-svgrepo-com.svg" alt="eye" id="ConfirmPasswordEyeImage" class="h-5 w-5">
                    <img src="/eye-password-hide-svgrepo-com.svg" alt="closeEye" id="ConfirmPasswordCloseEyeImage" class="h-5 w-5 hidden">
                </span>
            </div>

            <!-- Submit Button -->
            <button type="submit"
                class="w-full bg-[#6d74fc] text-white font-semibold py-3 rounded-lg mt-5 hover:bg-[#5e63d0]">
                Reset Password
            </button>
        </form>

        <!-- Login Link -->
        <p class="text-center text-sm text-gray-500 mt-4 px-2">
            Did you succeed in resetting your password?
            <a href="/login" class="text-[#6d74fc] font-semibold">Log In</a>
        </p>
    </div>

</body>
    <script src="/resetPassword/script.js"></script>
</html>